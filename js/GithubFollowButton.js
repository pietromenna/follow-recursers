var React = require('react');

var GithubFollowButton = React.createClass({
  render: function () {
    return <button type="button">Follow on Github</button>
  }
});

module.exports = GithubFollowButton;
